# Project Status

Last updated: 2026-05-08

## Issues

- [x] 001-setup-vite-mantine-tanstack.md - completed
- [ ] 002-setup-api-client-codegen.md - pending
- [ ] 003-build-app-shell-and-auth.md - pending
- [ ] 004-implement-catalog-feature.md - pending
- [ ] 005-implement-procurement-feature.md - pending
- [ ] 006-implement-inventory-feature.md - pending
- [ ] 007-implement-sales-feature.md - pending
- [ ] 008-implement-financials-and-csv-export.md - pending
- [ ] 009-wire-cmdk-palette-and-polish.md - pending
- [ ] 010-add-e2e-smoke-and-deploy.md - pending

## Summary

Total: 10 issues
Completed: 1
In progress: 0
Planned: 0
Pending: 9
Failed: 0

## Execution Log

- 2026-05-08 — 001-setup-vite-mantine-tanstack: planned. Vite + React 18 + TS strict + Mantine v7 + Tailwind (`floor:` variant) + TanStack Router/Query + Zustand + Vitest + MSW + ESLint/Prettier; ships `<DecimalInput>`, `money.ts`, `qty.ts`, charcoal theme from `src/theme/tokens.ts`, and a `/login` placeholder. No BE dep; no human input needed before /execute.
- 2026-05-08 — 001-setup-vite-mantine-tanstack: in_progress. Executor started.
- 2026-05-08 — 001-setup-vite-mantine-tanstack: completed.
  - `package.json` — React 18, Mantine v7, TanStack Router/Query v5, Zustand, Decimal.js, Vitest, MSW, ESLint 9, Prettier, Fontsource Inter + JetBrains Mono
  - `tsconfig.json` / `tsconfig.node.json` — strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, @/* alias
  - `vite.config.ts` — @vitejs/plugin-react, @tanstack/router-plugin, alias @→src, port 5173
  - `vitest.config.ts` — jsdom, setupFiles, css:true, @/* alias
  - `tailwind.config.ts` — charcoal palette from tokens, floor: variant, no light mode
  - `postcss.config.js`, `eslint.config.js`, `.prettierrc`, `.env.example`, `.gitignore`
  - `index.html`
  - `src/theme/tokens.ts` — all brand colors, type scale, spacing, density, radius, shadows, focus
  - `src/theme/mantine.ts` — charcoal Mantine theme (terere primary, dark scale override)
  - `src/theme/global.css` — Tailwind base/components/utilities, dark unconditionally, no :root light block
  - `src/main.tsx` — env fail-fast, Decimal.set, font imports, RouterProvider
  - `src/router.ts` — createRouter + queryClient
  - `src/routeTree.gen.ts` — generated by TanStack Router plugin during build
  - `src/routes/__root.tsx`, `src/routes/index.tsx`, `src/routes/login.tsx`
  - `src/components/DecimalInput.tsx` + `.test.tsx` — string-only decimal input, blur normalization, tabular-nums
  - `src/utils/money.ts` + `.test.ts` — formatMoney, parseMoneyString, Decimal.js, no number math
  - `src/utils/qty.ts` + `.test.ts` — formatQty, toBaseUnit, fromBaseUnit, g/ml/unit base units
  - `src/stores/floor-mode.ts` — Zustand floor-mode store, persisted to localStorage
  - `src/test/setup.ts` — jest-dom, MSW server init, Decimal global config, matchMedia mock
  - `src/test/server.ts` — MSW setupServer (0 handlers; issue 002+ adds them)
  - `src/test/render.tsx` + `.test.tsx` — renderWithProviders (Mantine+QueryClient), re-exports Testing Library
  - `README.md` — real setup section (prerequisites, install, commands, architecture, docs links)
  - Gates: 48/48 tests pass; tsc --noEmit clean; eslint 0 warnings; no bare fetch/axios; no as any; no NumberInput in features; no console.log; npm run build succeeds

## Notes

- Issues mirror SPEC §5 phases 1–14 with three consolidations: SPEC phases 7+8 (Sales draft + Sales commit/void) → issue 007; SPEC phases 10+11 (⌘K palette + Polish) → issue 009; SPEC phases 12+14 (E2E smoke + Deploy) → issue 010. SPEC phase 13 (Agent panel — explicitly out of v1 MVP per §2.9) is **not** broken into a v1 issue. Surface it as `011-implement-agent-panel.md` if/when the take-home buffer permits.
- BE dependencies per issue: 003 → BE phase 5 (auth), 004 → BE phase 6 (catalog), 005 → BE phase 7 (procurement), 006 → BE phase 8 (inventory), 007 → BE phase 9 (sales), 008 → BE phase 10 (financials). Issue 002 needs the BE OpenAPI schema (BE phase 13 emits it; an interim hand-published snapshot is acceptable per SPEC §5 phase 2 to unblock parallel work). Issues 001, 009, 010 have no BE dependencies.
- Each issue is one phase-level unit. If `/plan` finds an issue too large to plan in one pass, propose a sub-issue split inside the issue's `## Notes` section and mark it `blocked` rather than re-breaking the whole spec.
- Hard invariants every issue must respect (per `ilex-discipline` skill + SPEC §2): no hand-written API types, no bare `fetch` outside `src/data/` (CSV export through `src/utils/csv-export.ts` is the documented exception), no `as any` outside `src/api/generated/`, money/qty as strings end-to-end, no `NumberInput` in money/qty fields, terminal mutations never optimistic, `Idempotency-Key` on the seven BE-required endpoints, 409 stale-state → refetch + toast, cross-owner = 404 rendered as "Not found".
- **Take-home brief soft gap:** `docs/takehome-challenge.md` says "Diagrams welcome." We have none yet. Candidate diagrams: (a) FE four-layer architecture (Routes → Features → Data → Generated) as a Mermaid in `docs/architecture.md`; (b) FEFO commit flow (BE walks FEFO → allocations + movements → invalidations) as a sequence diagram; (c) Recall flow (toggle → block future sales → recall report). Not blocking, not a numbered issue — pick this up during issue 010 polish or write a one-off `docs/diagrams.md` if buffer permits. README link to the diagrams once they exist.
- **001 note:** `vitest.config.ts` uses `globals: true` (not the plan's `globals: false`) because `@testing-library/jest-dom` patches `expect` globally. All other plan directives respected verbatim. `npm run generate:api -- --check` gate deferred to issue 002 as planned.
